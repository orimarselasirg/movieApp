import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  footerLoader: {
    paddingVertical: 20,
    alignItems: 'center',
    width: '100%',
  },
  footerText: {
    color: '#67686D',
    fontSize: 14,
    marginTop: 10,
  },
});